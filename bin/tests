#!/usr/bin/env ruby

require 'cryptobroker'
require 'cryptobroker/indicator/dema'
require 'cryptobroker/broker/basic'
require 'cryptobroker/downloader'
require 'cryptobroker/chart'

app = Cryptobroker.new('../config.yml')
app.invest
loop { sleep 60 }

# api = app.api 'cexio'
# couple = 'BTC/USD'
# archived = api.archived_orders(couple).select { |o| o.base_change != 0 }.first
# puts JSON.pretty_generate(archived.as_json)
